// Types for the scroll-experience slice (GSAP ScrollTrigger + Lenis smooth scroll reveal).

/** Progress through the pinned hero timeline, 0 at the top and 1 at the end. */
export type ScrollProgress = number;
