export function SiteFooter() {
  return (
    <footer className="border-t border-sage bg-cream">
      <div className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-4 px-6 py-10 sm:px-10">
        <span className="font-display text-lead font-semibold text-forest">
          realsign
        </span>
        <p className="font-body text-caption text-forest/75">
          Recognition runs in your browser. Only the recognized letters are sent
          for smoothing.
        </p>
      </div>
    </footer>
  );
}
