// Types for the landing slice (marketing surface around the interpreter).

/** One honest boundary, stated in the interface rather than buried in docs. */
export interface Limit {
  title: string;
  body: string;
}
